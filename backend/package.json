{
  "name": "split-bill-backend",
  "version": "1.0.0",
  "description": "Backend server for Split Bill App",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "npx tsc --noEmit",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@codebygarv/express-lens": "^1.0.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.3.1",
    "nodemailer": "^9.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
